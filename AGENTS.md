# AGENTS.md

本文件适用于整个仓库。

## 项目目标

本项目用于记录和整理对 DeepSeek Harness（DSH）的理解、学习过程、实验结果和信息图成果。主要内容形式为 Markdown 文档和图片。

本项目同时服务于以下两个目标，保留用户原话：

1. 我作为工程师，想用好玩好deepseek harness，这篇论文应该通俗理解哪些内容，理解到什么程度
2. 我作为抖音博主，想给大家讲解背后的原理，应该讲解哪些内容，怎么讲解。

以下内容是对上述目标的执行要求：

### 工程师学习目标

- 以“能够正确使用和扩展 DeepSeek Harness”为目的，通俗理解论文，而不是默认逐句掌握全部形式化证明。
- 优先吃透与实际工程直接相关的内容：插件或 Component 的结构，Context，dependency、provision、effect，inverse 或 disposer，reactive coeffect，Provider / Consumer 生命周期，withdrawal ordering，以及系统边界。
- 理解应达到能够用自己的话解释概念、把论文概念对应到 DSH 源码、通过最小实验验证关键行为，并判断哪些效果可撤销、哪些外部行为只能补偿的程度。
- 对定理先掌握结论、成立条件和工程含义；Monad、comonad、代数证明及 operational semantics 等形式化细节，只有在修改 Cordis 内核或专门学习编程语言理论时再深入。

### 抖音内容目标

- 以“让没有论文背景的观众理解 DSH 背后的原理”为目的，讲清为什么需要 Cordis，以及 temporal composability 和 spatial composability 分别解决什么问题。
- 重点讲插件留下的 effect 为什么需要 cleanup、依赖出现或消失时组件如何响应、Provider 退出时为什么要让 Consumer 先退出，以及系统边界外的行为为什么不能自动撤销。
- 优先使用一个贯穿始终的插件案例、生活化类比、加载与卸载的过程图和“能保证什么／不能保证什么”的对照来讲解，避免从术语定义或形式化证明起讲。
- 对外表达必须区分论文结论、源码事实、实验观察和个人推断；不得把“可组合、可撤销”夸大成“能撤销所有副作用”或“DSH 已实现可靠的自我进化”。

## 目录约定

- 未整理的临时笔记放入 `notes/inbox/`。
- 概念笔记按主题放入 `notes/concepts/`，避免按日期堆放。
- 源码阅读记录放入 `notes/source-reading/`。
- 可复现的验证过程放入 `labs/<编号-主题>/`。
- 已经形成完整表达的内容放入 `handbook/`。
- 信息图放入 `visuals/<主题>/`；如有可编辑源文件，应与导出图片放在同一主题目录。
- 参考资料和引用索引放入 `references/`。
- 被替代但仍需保留的内容放入 `archive/`，不得直接删除。
- 不创建 Upstream 跟踪文件，除非用户明确要求。

## 命名约定

- 目录使用小写英文和连字符，例如 `context-session/`。
- Markdown 正文文件可以使用清晰的中文标题。
- 实验目录使用三位编号，例如 `001-first-run/`。
- 同一主题只保留一份主要说明，其他文档通过链接引用，避免复制形成多个事实源。

## 写作约定

- 默认使用中文写作，技术名词保留必要的英文原名。
- 明确区分事实、观察、推断和个人理解。
- 实验记录应说明目的、环境、步骤、结果和结论。
- 尚未验证的判断不得写成确定事实。
- 信息图中的关键结论应能追溯到相应笔记、实验或参考资料。

## 修改边界

- 保持改动范围小，不顺手重写无关内容。
- 未经用户明确要求，不移动、归档或删除已有学习内容。
- 未经用户明确要求，不提交、推送、发布或部署。
- 只要求初始化目录时，不添加占位正文或示例学习内容。
