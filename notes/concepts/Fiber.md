# Fiber

`Fiber` 可以理解成：

> **一个插件被挂载以后，在运行时产生的“活实例 + 生命周期档案”。**

现阶段看到 Fiber，只需要问五个问题：

1. 它对应哪个 Plugin？
2. 它当前是什么状态？
3. 它在等待哪些依赖？
4. 它拥有并需要清理哪些 effects？
5. 它是谁的子 Fiber？

能回答这五个问题，就已经足够用于 DSH 插件排错和扩展。
